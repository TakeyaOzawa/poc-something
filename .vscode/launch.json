{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Amazon Q CLI",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/chat_cli",
      "args": ["chat"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
