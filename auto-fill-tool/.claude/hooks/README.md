# Claude Code Hooks & Automation

このディレクトリには、開発ワークフローを自動化するためのコマンドとガイドラインが含まれています。

## 📋 利用可能なワークフロー

### 1. 品質ゲート完全チェック

開発完了時の6ステップ品質保証プロセスを実行します。

```bash
cd auto-fill-tool

# ステップ1: カバレッジ測定
npm run test:coverage

# ステップ2: テスト実行
npm test

# ステップ3: Lint自動修正
npm run lint:fix && npm run format

# ステップ4: Lintチェック
npm run lint

# ステップ5: ビルド実行
npm run build
```

**完了基準:**
- ✅ カバレッジ: 修正範囲が90%以上
- ✅ テスト: 0 failed
- ✅ Lint: 0 errors, 0 warnings
- ✅ ビルド: 成功

### 2. カバレッジチェック & テスト強化

テストカバレッジを測定し、90%未満の場合はテスト追加を行います。

```bash
cd auto-fill-tool

# カバレッジ測定
npm run test:coverage

# カバレッジが90%未満の場合、以下のテストケースを追加：
# 1. 正常系テスト
# 2. 異常系テスト
# 3. 境界値テスト
# 4. 分岐網羅
```

### 3. Gitコミット品質チェック

コミット前にアーキテクチャ準拠と品質基準を確認します。

```bash
cd auto-fill-tool

# ステップ1: アーキテクチャ準拠確認
npm run lint:architecture

# ステップ2: 変更ファイルのテスト実行
git diff --cached --name-only --diff-filter=AM | grep '\\.ts$' | grep -v '\\.test\\.ts$' | head -5 | xargs -I {} dirname {} | sort | uniq | xargs -I {} npm test -- --testPathPattern="{}" --passWithNoTests

# ステップ3: 変更ファイルのLintチェック
git diff --cached --name-only --diff-filter=AM | grep '\\.ts$' | xargs npm run lint --

# ステップ4: 型チェック
npm run type-check

# ステップ5: 循環依存チェック
npm run analyze:circular
```

### 4. エラーハンドリング強化

エラーコード管理とStandardError使用のガイドを提供します。

```bash
cd auto-fill-tool

# エラーコード一覧表示
npm run error:list

# 新エラーコード予約
npm run error:reserve
```

### 5. 多言語リソース更新

i18nメッセージキーの追加・更新を行います。

```bash
cd auto-fill-tool

# メッセージファイルの編集
# public/_locales/en/messages.json
# public/_locales/ja/messages.json
```

**メッセージキー命名規則:**
```json
{
  "feature_action_element": {
    "message": "メッセージ内容",
    "description": "このメッセージの用途説明"
  }
}
```

## 🔔 Slack通知

ユーザーへの質問や重要な通知の前に、以下のコマンドでSlack通知を送信します：

```bash
bash slackNotification.sh "[$$]タスク要約" "状況詳細"
```

**環境変数設定:**
```bash
export MY_SLACK_OAUTH_TOKEN='xoxb-your-bot-token'
export MY_SLACK_USER_ID='U1234567890'
```

## 📊 統合検証コマンド

```bash
# ローカル検証
npm run validate

# 完全なCI相当の検証
npm run ci
```

## 🔄 推奨Gitワークフロー

```bash
# 1. 変更をステージング
git add .

# 2. コミット品質チェック実行（上記参照）

# 3. 問題がなければコミット
git commit -m "feat(domain): 新機能を追加"

# 4. プッシュ
git push origin feature/new-feature
```

## 📚 参考資料

- **品質基準**: `.claude/rules/quality-standards.md`
- **プロジェクト構造**: `.claude/rules/project-structure.md`
- **技術スタック**: `.claude/rules/tech-stack.md`
- **MCP統合**: `.claude/rules/mcp-integration.md`
